import {
  SEED_TENANTS,
  SEED_USERS,
  SEED_CONSULTOR_TENANTS,
  SEED_SKUS,
  SEED_COMPETIDORES,
  SEED_R001,
  SEED_R002,
  SEED_R004,
  SEED_R005,
  SEED_R006,
  SEED_R007,
  SEED_R008,
  SEED_R009,
  type MockTenant,
  type MockUser,
  type MockConsultorTenant,
  type MockSku,
  type MockCompetidor,
  type MockCategoriaVP,
  type MockElasticidad,
  type MockColConfig,
  type MockCanalesMargenes,
  type MockUmbrales,
  type MockPesoItem,
} from './data'

export const store = {
  tenants: structuredClone(SEED_TENANTS) as MockTenant[],
  users: structuredClone(SEED_USERS) as MockUser[],
  consultorTenants: structuredClone(SEED_CONSULTOR_TENANTS) as MockConsultorTenant[],
  skus: structuredClone(SEED_SKUS) as MockSku[],
  competidores: structuredClone(SEED_COMPETIDORES) as MockCompetidor[],
  r001: structuredClone(SEED_R001) as Record<string, string[]>,
  r002: structuredClone(SEED_R002) as MockCategoriaVP[],
  r004: structuredClone(SEED_R004) as MockElasticidad[],
  r005: structuredClone(SEED_R005) as MockColConfig,
  r006: structuredClone(SEED_R006) as MockColConfig,
  r007: structuredClone(SEED_R007) as MockCanalesMargenes,
  r008: structuredClone(SEED_R008) as MockUmbrales,
  r009: structuredClone(SEED_R009) as MockPesoItem[],
  _idCounter: 100,
}
